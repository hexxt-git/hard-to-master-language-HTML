psh  int -> pushes a number to the top of the stack
pop      -> removes the top number from the stack
add      -> adds up   the top two numbers of the stack
sub      -> subtracts the top two numbers of the stack
ife  int -> if the top isnt =int skips the next inst
ifg  int -> if the top isnt >int skips the next inst
jmp  lbl -> jumps the program to a label
lbl:     -> ends the previous label and begins a new one
hlt      -> halts the label
red      -> reads and int input
wri  msg -> writes a message
prt      -> writes the top of the stack
html programs start at main function and stop if they run-
out of instructions or reach a halt at main leve